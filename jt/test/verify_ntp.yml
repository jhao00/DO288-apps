- name: gather facts using netcommon
  hosts: test
  gather_facts: no
  become: yes
  become_method: enable

  tasks:
  - name: gather facts using  netcommon
    ansible.netcommon.network_resource:
    register: out

  - name: debug
    debug:
      var: out

        #  - name: add task
        #ansible.builtin.include_tasks: ntp/verify_ntp.yml

  - name: using netcommon
    become: yes
    become_method: enable
    ansible.netcommon.network_resource:
      name: ntp_global
      state: gathered
    register: out
  
  - name: debug
    debug:
      var: out
   
