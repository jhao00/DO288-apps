---
ansible-navigator:
   ansible:
     inventory:
       entries:
         - inventory.yaml
   execution-environment:
     image: hub.lab.example.com/ee-supported-rhel8:latest
     pull:
       policy: missing
   logging:
     level: debug
     append: False
   mode: stdout
   playbook-artifact:
     enable: False

