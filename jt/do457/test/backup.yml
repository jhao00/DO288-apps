- name: gather facts subset
  hosts: ios,
  gather_facts: no
  become: no

  tasks:
    - name: backup running-configs
      vars:
        ansible_connection: ansible.netcommon.network_cli
      ansible.netcommon.cli_config:
        backup: true
        backup_options:
          filename:  "{{ inventory_hostname_short }}.txt"
