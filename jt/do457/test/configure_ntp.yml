- name: Remove NTP server
  hosts: test
  gather_facts: no
  become: true
  become_method: enable

  tasks:
    - name: remove ntp server
      ansible.netcommon.network_resource:
        name: ntp_global
        config:
          servers:
            -  server: 172.25.254.254
        state: deleted

        
