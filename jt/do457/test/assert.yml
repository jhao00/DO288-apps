- name: Verify changes
  hosts: test
  gather_facts: no
  become: no

  vars:
    test: "1.1.1.1.1"

  tasks:
    - name: test assert
      ansible.builtin.assert:
        that:
          - test is defined
          - test | ansible.utils.ipaddr('address') != False
