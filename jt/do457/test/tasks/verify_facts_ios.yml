- name: verify ios snmp
  cisco.ios.ios_facts:
    gather_subset:
      - interfaces

- name: debug
  ansible.builtin.debug:
    var: ansible_facts['net_interfaces']
    #    var: ansible_facts['net_config'] | regex_findall(action, multiline=true)
